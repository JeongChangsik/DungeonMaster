# 밤샘 작업 기록

브랜치: `feature/vampire-survivors-content`
목표: 뱀서라이크 **콘텐츠 확장 + 타격감**. 게임 루프(게임오버/결과화면)는 범위 밖.
방침: 무한 생존, 기능별 커밋, `main` 미수정, 완료 후 `origin` push.

---

## 한눈에 보기

**게임에 들어간 것**

| | 내용 |
|---|---|
| 무기 | 공전 칼날 / 투척 단검 / 화염 오라 / 부메랑 도끼 — **4종** |
| 적 | 슬라임 / 고블린 / 오크전사 / 오크주술사 / 거대좀비 — **5종** |
| 적 행동 | 직진 추격 / 돌진 / 원거리 — **3종** |
| 업그레이드 카드 | **23종** (무기 강화 + 플레이어 패시브 + 체력 재생) |
| 난이도 | 600초에 걸쳐 상승. 스폰 주기·마릿수·적 능력치가 함께 오름 |
| 타격감 | 데미지 숫자 / 피격 플래시 / 적 체력바 |
| 사운드 | 적 피격·사망, 코인, 레벨업, 카드 선택, 투척, 플레이어 피격 |
| 성능 | 오브젝트 풀링 (적·코인·투사체). 최대 부하에서 풀 증식 0 |

**직접 만지실 만한 곳**

| 바꾸고 싶은 것 | 어디 |
|---|---|
| 카드 효과·최대 횟수 | `Assets/02_Scripts/Character/Data/` 의 각 `.asset` |
| 난이도 곡선 | 씬의 `EnemySpawner` (전부 인스펙터 노출) |
| 적 스탯·행동 | `RL___SO.asset` (체력/속도/피해/행동유형) |
| 무기 수치 | 씬의 `WeaponRoot` 아래 각 무기 컴포넌트 |
| 효과음 | `Core/Data/AudioDataSO.asset` (미사용 클립 50개 이상 남아 있음) |

**아직 없는 것 (범위 밖이라 안 만들었습니다)**

- **게임오버** — 플레이어 HP 가 0 이 되어도 아무 일도 없습니다. `Player.Die()` 가 로그 한 줄뿐입니다.
- 결과 화면, 재시작, 일시정지, 메타 진행(영구 강화·저장)

**확인 부탁드리는 것**

- 씬에 **직접 배치된 적 4마리**(`RL_Swampy`, `_1`, `_2`, `_3`)가 남아 있습니다.
  게임 시작하자마자 근처에 서 있게 됩니다. 테스트 잔재로 보이는데 제가 임의로 지우지 않았습니다.
- `RLSwampySO` 공격력을 10 → 16 으로 올렸습니다. 방어력이 10 이라 실제 피해가 1 밖에
  안 들어가서 게임이 성립하지 않았습니다.
- `ProjectSettings.runInBackground` 를 켰습니다. 자동 플레이 테스트에 필요했습니다.

---

## 아침에 먼저 봐주실 것

### ⚠️ 승인해주신 계획에서 벗어난 판단

**무기 추상화를 "만들지 않는다" → "가볍게 만든다" 로 변경했습니다.**

계획서에는 `WeaponManager`/`WeaponBase` 를 만들지 않겠다고 적었는데, 설계를 더 파보니
그 판단으로는 막히는 지점이 두 개 있었습니다.

1. **"모든 무기 피해량 +10%" 같은 전역 패시브를 만들 수가 없습니다.** 무기 전체를 훑을
   주체가 없어서, 카드 SO 마다 `GetComponentsInChildren` 을 중복 호출해야 합니다.
2. **SO 클래스가 폭발합니다.** 무기 4종 × 스탯 4개 = `.cs` 파일 16개가 됩니다.

그래서 `WeaponBase`(추상 클래스) + `WeaponManager`(무기 조회/일괄 갱신) 만 두고,
카드 SO 는 **`WeaponUpgradeSO` 1개 + `PlayerStatUpgradeSO` 1개, 총 2개 클래스로 전부** 커버합니다.
새 무기를 추가할 때 `LevelUpUI.cs` / `LevelUpCard.cs` / 카드 프리팹은 **여전히 손대지 않습니다.**

되돌리고 싶으시면 커밋 `C3` 부터 revert 하시면 됩니다.

---

## 커밋 기록

### C0 — 잔여 배선 마무리 + 백그라운드 실행 (기준선)

**한 일**

- `LevelUpUI.Starting Upgrades` 에 `칼날 추가` 연결
- `OrbitPivot.WeaponOrbit.Count` 를 `1` → `0` 으로 변경
  (시작 칼날을 씬이 아니라 `Starting Upgrades` 가 만들도록. 그래야 레벨과 개수가 일치)
- **`ProjectSettings.runInBackground` 를 켰습니다** — 아래 참조

**발견한 문제: Unity가 백그라운드에서 멈춰 있었습니다**

`runInBackground` 가 꺼져 있어서, Unity 창이 포커스를 잃으면 플레이 모드가 **2프레임만 돌고
정지**했습니다. 밤새 자동으로 플레이 테스트를 하려면 이게 켜져 있어야 해서 프로젝트 설정을
변경했습니다.

> 게임 플레이에는 영향이 없습니다. 오히려 빌드에서도 창이 백그라운드로 갈 때 게임이 멈추지
> 않게 되므로 일반적으로 켜두는 쪽이 맞습니다. 원치 않으시면
> `Edit > Project Settings > Player > Resolution and Presentation > Run In Background` 를 끄시면 됩니다.

**검증한 것 (플레이 모드에서 실측)**

| 항목 | 결과 |
|---|---|
| 시작 시 칼날 개수 | 1개 ✓ |
| 시작 로그 | `업그레이드 적용: 칼날 추가 Lv.1` ✓ |
| 레벨업 발생 | `레벨업! 현재 레벨:2` ✓ |
| 카드 표시 | `칼날 추가 Lv.1` / `칼날 데미지 Up Lv.0` — **서로 다른 아이콘** ✓ |
| 후보 부족 시 | 3번째 카드 자동 숨김 ✓ |
| 카드 선택 후 | 칼날 1 → 2개, `timeScale` 1 로 복귀 ✓ |
| 연속 레벨업 | 잔여 경험치로 카드가 한 번 더 뜸 ✓ |
| 적 스폰 | 4마리 정상 ✓ |
| 에러/경고 | **0건** ✓ |

어젯밤 내내 잡으셨던 카드 시스템이 **끝에서 끝까지 정상 동작**하는 것을 확인했습니다.

---

### C1 — 플레이어 스탯 보정 레이어

레벨업 카드로 이동속도/최대체력을 올릴 수 있게 하는 기반 작업.

`Player.cs` 의 기존 `[SerializeField]` 값은 **기본값으로 그대로 두고**, 강화분만 별도 필드에
누적한 뒤 프로퍼티에서 합쳐서 돌려줍니다. 보정치 초기값이 0/1 이라 **구 GamePlay 씬의 동작은
수치적으로 완전히 동일**합니다.

**작업 중 발견한 버그 (계획에 없던 것)**

`InputHandler` 가 `performed`/`canceled` 에만 바인딩되어 있어서 `OnMove` 는 입력이 **바뀔 때만**
호출됩니다. 그래서 이동속도를 강화해도 **키를 뗐다 다시 누르기 전까지 반영되지 않았습니다.**
마지막 입력을 `_moveInput` 에 기억하고 `ApplyMoveVelocity()` 로 속도 적용을 한 곳에 모아
해결했습니다.

실측: 키를 누르고 있는 상태에서 `AddMoveSpeed(+50%)` → `linearVelocity` 가 (5,0) → (7.5,0) 즉시 변경.

---

### C2 — 플레이어 스탯 카드 5종 (카드 후보 2 → 7종)

| 카드 | 효과 | 최대 |
|---|---|---|
| 최대 체력 증가 | +25 (늘어난 만큼 즉시 회복) | 5회 |
| 이동 속도 증가 | +12% | 5회 |
| 자석 범위 증가 | +1.2 | 4회 |
| 경험치 획득 증가 | +15% | 5회 |
| 공격 속도 증가 | 쿨다운 −8% | 5회 |

`PlayerStatUpgradeSO` **클래스 하나**로 5종을 전부 만들었습니다. 새 스탯을 추가할 때
`.cs` 파일을 만들 필요 없이 enum 한 줄 + case 한 줄이면 됩니다.

실측: 레벨업 시 서로 다른 카드 3장이 각자 다른 아이콘으로 표시됨.
자석 카드 선택 시 `PickupRadius` 3 → 4.2 반영 확인.

**밸런스는 제가 임의로 정한 값입니다.** 아침에 플레이해보시고 감이 안 맞으면
`Assets/02_Scripts/Character/Data/` 의 각 `.asset` 에서 `Flat`/`Percent`/`Max Level` 만
바꾸시면 됩니다. 코드는 안 건드려도 됩니다.

**부수 작업**: `frames/` 의 아이콘용 PNG 5장이 PPU 100 + Bilinear 기본값이라 픽셀아트가
뭉개져서 PPU 16 + Point + 무압축으로 임포트 설정을 교정했습니다. 앞으로 추가할
몬스터/무기 스프라이트도 같은 교정이 필요합니다.

---

### C3 — 무기 확장 기반 (WeaponBase / WeaponManager), 카드 10종

투사체·오라 무기를 붙일 자리를 만드는 작업입니다. 핵심은 **새 무기를 추가할 때
`LevelUpUI.cs` / `LevelUpCard.cs` / 카드 프리팹을 영원히 안 건드려도 되게** 하는 것입니다.

새 무기 추가 절차가 이렇게 됩니다.
1. `WeaponOOO.cs` 한 개 작성 (`WeaponBase` 상속)
2. `WeaponId` enum 에 한 줄 추가
3. `WeaponRoot` 아래 빈 오브젝트에 붙이기
4. `WeaponUpgradeSO` 에셋 2~3개 생성

**기존 것이 하나도 안 깨졌습니다.** `WeaponOrbit` 의 `_damage` 를 부모 클래스로 옮겼는데도
직렬화 키 이름이 같아서 씬에 저장된 값 10 이 그대로 유지되는 걸 실측으로 확인했습니다.
`AddCount`/`AddDamage`/`AddRadius`/`MultiplySpeed` 시그니처도 그대로 둬서
`칼날 추가.asset`, `칼날 데미지 증가.asset` 이 무수정으로 동작합니다.

**"모든 무기 피해량 +10%" 가 가능해졌습니다.** 실측에서 전역 배율 +50% 를 적용하니
이미 돌고 있던 칼날 3개 전부의 피해량이 10 → 15 로 갱신됐습니다.
`WeaponManager` 없이는 이걸 할 주체가 없습니다.

추가된 카드 3종

| 카드 | 효과 | 최대 |
|---|---|---|
| 칼날 궤도 확장 | 궤도 반경 +0.5 | 4회 |
| 칼날 회전 가속 | 회전 속도 +20% | 4회 |
| 전체 무기 피해 증가 | 모든 무기 피해 +10% | 5회 |

**편의 개선**: `LevelUpUI` 의 `Pool` 은 이제 `Data` 폴더의 `UpgradeSO` 를 자동으로 모아
채웁니다. 앞으로 카드 에셋을 만들면 인스펙터에 수동 등록할 필요가 없습니다.

---

### C4 — 적 3종 추가 (코드 수정 0줄)

`RoguelikeEnemy.cs` 를 **한 줄도 안 고치고** 적을 4종으로 늘렸습니다.
애니메이터 파라미터 규약(`IsWalk` bool + `Hit` trigger)을 기존 `RLSwampyAnim` 과
똑같이 맞췄기 때문에 코드 입장에서는 어떤 몬스터든 구분이 없습니다.

| 적 | 체력 | 속도 | 피해 | 성격 |
|---|---|---|---|---|
| RL_Swampy (기존) | 30 | 2.5 | 16 | 기본 |
| RL_Goblin | 15 | 4.0 | 14 | 빠르고 약함 |
| RL_OrcWarrior | 45 | 2.2 | 22 | 표준 |
| RL_BigZombie | 120 | 1.3 | 40 | 느리고 단단함 |

**밸런스 수정 하나 — 봐주셨으면 합니다.**

`RLSwampySO` 의 공격력을 **10 → 16** 으로 올렸습니다.
플레이어 방어력이 10 이라 실제 피해가 `max(1, 10-10) = 1` 밖에 안 들어가서,
적에게 계속 맞아도 사실상 죽지 않는 상태였습니다. 16 이면 실제 피해 6 입니다.
원치 않으시면 `RLSwampySO.asset` 에서 되돌리시면 됩니다.

**만드는 절차** (다음에 몬스터를 더 추가하실 때 참고)
1. `frames/` 에서 `xxx_idle_anim_f0~3`, `xxx_run_anim_f0~3` 8장 선택
2. 임포트 설정을 **PPU 16 + Point + 무압축** 으로 교정 (기본값이 PPU 100 + Bilinear 라 뭉개짐)
3. 클립 2개(Idle/Walk, 8fps 루프) + AnimatorController 1개 생성
4. `RL_Swampy` 를 복제해 스프라이트·컨트롤러·EnemySO·콜라이더 반경만 교체
5. `EnemySpawner._enemyPrefabs` 에 등록

전부 스크립트로 자동화해서 처리했습니다.

실측(15초 플레이): 4종 전부 스폰. 이동속도가 1.30 / 2.50 / 4.00 으로 SO 값대로 반영.
Walk 애니메이션 재생 확인. 에러 0건.

---

### C5 — 타격감 (데미지 숫자 / 피격 플래시 / 적 체력바)

Feel 에 기성품이 다 있어서 새로 쓴 코드는 최소입니다.

| 연출 | 사용한 것 |
|---|---|
| 데미지 숫자 | Feel `MMFloatingTextPixel` 프리팹 + `MMFloatingTextSpawner` (풀 60개) |
| 적 체력바 | Feel `MMHealthBar` (Drawn 모드 — 프리팹 제작 불필요) |
| 피격 플래시 | 직접 구현 (0.08초) |

**시행착오 하나 — 흰색 플래시가 안 보였습니다.**

처음에 피격 시 흰색으로 번쩍이게 만들었는데 화면상 아무 변화가 없었습니다.
`SpriteRenderer.color` 는 **곱연산**이고 스프라이트 기본 색이 흰색이라
흰색을 곱해도 그대로이기 때문입니다. 진짜 흰색 발광을 하려면 전용 셰이더가 필요합니다.
대신 **붉게 물들이는 방식**(1, 0.35, 0.35)으로 바꿨습니다.

나중에 제대로 된 흰색 발광을 원하시면 스프라이트 전용 플래시 셰이더를 하나 만들어
`_flashColor` 대신 머티리얼을 교체하는 방식으로 바꾸면 됩니다.

**체력바 설정 중 주의할 것**

- `NestDrawnHealthBar = true` 로 뒀습니다. `false`(기본값)면 체력바가 적의 자식이 아니라
  별도 오브젝트로 생성돼서 **적이 죽어도 바만 화면에 남습니다.**
- `AlwaysVisible = false`. 화면에 적이 20마리씩 있는데 바가 항상 떠 있으면 너무 시끄러워서,
  맞았을 때만 1.5초간 보이게 했습니다.
- `SortingLayerName` 기본값이 `"UI"` 인데 이 프로젝트엔 그 정렬 레이어가 없어서 `"Default"` 로 바꿨습니다.

**히트스톱은 일부러 넣지 않았습니다.**

계획서에는 넣기로 했는데, 실제로 보니 화면에 적이 20마리씩 있고 공전 칼날이 쉬지 않고
때리는 구조라 **매 타격마다 히트스톱을 걸면 게임 전체가 슬로우모션**이 됩니다.
뱀서라이크류가 히트스톱을 안 쓰는 이유이기도 합니다. 보스처럼 드물게 발생하는
큰 타격에만 쓰는 게 맞아서, 보스를 추가할 때 같이 넣는 게 낫다고 판단했습니다.

실측: 적 20마리 전부 체력바 생성, 피격 시 색 변화 후 복귀, 데미지 숫자 4개 동시 표시.

---

### C6 — 카메라 링 스폰 + 시간축 난이도 (무한 생존)

두 가지 문제를 같이 해결했습니다.

**문제 1 — 1분이나 10분이나 똑같았습니다.** 스폰 주기·마릿수·적 스탯이 전부 고정이라
무한 생존이 성립하지 않았습니다.

**문제 2 — 적이 20초씩 걸어왔습니다.** 맵 가장자리(428지점)에서만 스폰돼서
플레이어가 맵 중앙에 있으면 적이 한참 걸어와야 했습니다.

**바뀐 것 — 카메라 링 스폰**

이제 화면을 감싸는 원 위에서 스폰합니다. 반경은 화면 대각선 절반 + 여유로,
현재 카메라(ortho 6, 16:9) 기준 약 **13.7** 입니다. 플레이어가 어디 있든
화면 밖에서 바로 다가옵니다.

링 위에 걸을 수 있는 칸이 없으면(플레이어가 맵 구석에 붙은 경우) 기존 가장자리
지점으로 자동 폴백합니다.

**바뀐 것 — 난이도 곡선 (600초에 걸쳐 상승)**

| | 시작 | 최대 |
|---|---|---|
| 스폰 주기 | 1.2초 | 0.18초 |
| 한 번에 | 1마리 | 4마리 |
| 동시 상한 | 30 | 160 |
| 적 체력 배율 | 1.0 | 4.0 |
| 적 피해 배율 | 1.0 | 2.5 |

**적 해금 시간표**

| 적 | 등장 시점 | 가중치 |
|---|---|---|
| RL_Swampy | 0초 | 3 |
| RL_Goblin | 45초 | 3 |
| RL_OrcWarrior | 120초 | 2 |
| RL_BigZombie | 240초 | 1 |

**이 숫자들이 전부 인스펙터에 노출되어 있습니다.** `EnemySpawner` 를 선택하면
`난이도 - ...` 항목들이 보이고, `Ramp Curve` 로 상승 모양(선형/급격/완만)도
바꿀 수 있습니다. 밸런스가 안 맞으면 여기 숫자만 만지시면 됩니다.

실측: 램프를 20초로 임시 단축해 확인. 난이도T 1.0 도달 시 스폰 주기 0.18,
동시 상한 160, 실제 적 스탯이 Swampy hp30→120 / dmg16→40 으로 반영.
45초에 고블린 해금되어 등장. 적 164마리 동시 생존에서 에러 0건.

**성능 참고**: 적 164마리 + 각자 체력바까지 떠 있는 상태에서 에러는 없었지만,
아직 오브젝트 풀링을 안 넣어서 Instantiate/Destroy 가 계속 발생합니다.
뒤쪽 커밋에서 풀링을 넣을 예정입니다.

---

### C7 — 두 번째 무기 '투척 단검' + 오브젝트 풀 실적용 (카드 15종)

무기가 공전 칼날 하나뿐이라 빌드 다양성이 없었습니다. 이제 두 종류입니다.

**C3에서 만든 기반이 값을 했습니다.** 무기를 추가하면서 `LevelUpUI.cs`,
`LevelUpCard.cs`, 카드 프리팹을 **한 줄도 안 고쳤습니다.**

**해금 시스템**

`투척 단검` 카드를 뽑기 전에는 강화 카드 4종이 후보에서 자동으로 빠집니다.
해금하면 강화 카드가 열리고, 해금 카드는 후보에서 사라집니다. 실측으로 확인했습니다.

| 카드 | 효과 | 최대 |
|---|---|---|
| 투척 단검 | 무기 해금 | 1회 |
| 단검 위력 강화 | 피해 +6 | 5회 |
| 단검 추가 발사 | 발사 +1 (부채꼴) | 3회 |
| 단검 연사 | 속도 +25% | 4회 |
| 단검 관통 | 관통 +1 | 3회 |

**오브젝트 풀을 처음으로 실제 사용했습니다.**

`Core/ObjectPool.cs` 는 완성되어 있었는데 호출부가 0건이었습니다. 이제 단검이 씁니다.
씬 루트에 `ObjectPool` 오브젝트를 배치하고 Knife 60개 / ExpCoin 120개를 프리웜합니다.
(코인 풀링 연결은 다음 커밋)

**구현하면서 조심한 것들**

- `ContactFilter2D.useTriggers = true` — 적 콜라이더가 트리거라서 이걸 안 켜면
  적이 하나도 안 잡힙니다. 처음에 놓치기 쉬운 부분입니다.
- `_released` 플래그 — 관통 투사체가 같은 프레임에 두 적을 맞히면, 풀에 반납한
  뒤에도 그 프레임의 나머지 `OnTriggerEnter2D` 가 계속 들어옵니다.
- `ObjectPool.Spawn` 은 위치만 설정하고 **회전은 건드리지 않습니다.**
  그래서 `Launch()` 에서 직접 회전을 맞춥니다.
- Unity 6 에서 `GetInstanceID()` 가 폐기되어 `HashSet<Collider2D>` 로 바꿨습니다.

**검증 중 헤맨 것 하나 (기록용)**

단검이 하나도 안 날아가서 한참 원인을 찾았는데, **레벨업 카드 화면이 떠 있어서
`timeScale`이 0이었던 것**이었습니다. 자동 검증할 때 `Time.time` 이 안 늘어나면
이걸 먼저 의심해야 합니다.

---

### C8 — 세 번째 무기 '화염 오라' + 경험치 코인 풀링 (카드 18종)

**무기 3종 체제가 완성됐습니다.** 이번에도 `LevelUpUI` / `LevelUpCard` / 카드 프리팹은
한 줄도 안 고쳤습니다.

| 무기 | 방식 | 강화 카드 |
|---|---|---|
| 공전 칼날 | 주위를 도는 칼 | 개수 / 피해 / 궤도 / 회전속도 |
| 투척 단검 | 최근접 적에게 발사 | 해금 / 피해 / 발사수 / 연사 / 관통 |
| 화염 오라 | 주변 범위 지속 피해 | 해금 / 피해 / 범위 |

**화염 오라 구현 메모**

콜라이더를 아예 쓰지 않습니다. 틱마다 `Physics2D.OverlapCircle` 한 번으로 처리합니다.
`OnTriggerStay2D` + 적별 쿨다운 딕셔너리 방식은 적이 죽거나 풀에 반납될 때
딕셔너리를 정리해야 해서 훨씬 복잡해집니다.

비주얼은 **유니티 내장 Knob 스프라이트**(원형)를 주황 반투명으로 깔고 반경에 맞춰
스케일을 자동 계산합니다. 새 이미지 에셋이 필요 없습니다.

**경험치 코인 풀링**

`Coin` 이 `IPoolable` 을 구현하고 `Instantiate/Destroy` → `Spawn/Release` 로 바뀌었습니다.

조심한 부분: 플레이어 탐색을 `Start()` 에서 `FindPlayer()` 로 분리해 `OnSpawnFromPool`
에서도 부르게 했습니다. `Start` 는 최초 1회뿐이라 풀에서 재사용될 때는 안 불리고,
씬을 다시 시작하면 **파괴된 옛 플레이어를 물고 있게 됩니다.**

실측: 적 5마리 처치 → 코인 5개가 풀에서 나옴 → 획득 후 사용중 5→1, 대기 115→119.
풀 자식 총계가 프리웜 180개 그대로 유지되어 재사용이 정상 동작합니다.

**밸런스 참고**: 오라는 범위 전체를 때리므로 피해를 5로 낮게 잡았습니다.
칼날(10)이나 단검(12)보다 약하지만 여러 명을 동시에 때립니다.

---

### C9 — 무적 시간 + 사운드 연결

**무적 시간 (밸런스상 꼭 필요했습니다)**

적이 겹치면 각자 자기 쿨타임으로 동시에 때려서 순식간에 죽는 구조였습니다.
한 번 맞으면 **0.6초 무적 + 스프라이트 깜빡임**을 넣었습니다.

`Player.cs` 가 아니라 **`RoguelikePlayer` 에만** 넣었습니다. `Player.cs` 에 넣으면
구 `GamePlay` 씬의 난이도까지 바뀌기 때문입니다.

실측: 1차 피격 30 → 방어력 10 적용해 20 감소 (108 → 88), 무적 중 3회 추가 피격은
전부 무시 (88 → 88).

**사운드 — 로그라이크 씬이 완전 무음이었습니다**

기존에 SFX 62개 중 3개만 쓰고 있었습니다. 미사용 자산을 붙였습니다.

| 상황 | 클립 |
|---|---|
| 적 피격 | 26_sword_hit_1 |
| 적 사망 | 24_orc_death_spin |
| 코인 획득 | 01_chest_open_1 |
| 레벨업 | 10_human_special_atk_1 |
| 카드 선택 | 03_crate_open_1 |
| 단검 투척 | 27_sword_miss_1 |
| 플레이어 피격 | 11_human_damage_1 |

**소음 방지가 핵심이었습니다.** 적 20마리를 동시에 때리면 같은 소리가 겹쳐서 귀가
아픕니다. `AudioManager.PlaySFX()` 가 두 가지를 합니다.

1. 같은 클립이 **0.05초 안에 다시 울리면 무시**
2. 재생마다 **피치를 조금씩 흔들어** 기계적으로 들리지 않게

`Time.unscaledTime` 을 쓰므로 레벨업으로 시간이 멈춰도 정상 동작합니다.

**클립 선택은 제 취향입니다.** 마음에 안 드시면
`Assets/02_Scripts/Core/Data/AudioDataSO.asset` 에서 바꾸시면 됩니다.
`06_Sounds` 에 아직 안 쓴 클립이 50개 넘게 있습니다.

**발견한 것 (범위 밖이라 그대로 둠)**

플레이어 HP 가 0 이 되어도 **아무 일도 일어나지 않습니다.** `Player.Die()` 가
`Debug.Log` 한 줄뿐이라 죽은 채로 게임이 계속됩니다. 게임오버는 이번 작업
범위 밖이라고 하셔서 손대지 않았습니다.

---

### C10 — 적 풀링 + 한글 폰트 깨짐 수정 + InputHandler 버그

계획의 마지막 항목이었는데, 하다 보니 **눈에 보이는 버그 두 개를 더 잡았습니다.**

#### 1) 적 오브젝트 풀링

`RoguelikeEnemy` 가 `IPoolable` 을 구현하고 죽을 때 파괴 대신 풀에 반납합니다.
프리웜 총 330개 (단검 60 / 코인 120 / 슬라임 60 / 고블린 40 / 오크 30 / 좀비 20).

`OnSpawnFromPool` 에서 **전부** 초기화합니다. 하나라도 빠뜨리면 이렇게 됩니다.
- 체력을 안 되돌리면 → 체력이 깎인 채로 스폰
- `_isDead` 를 안 되돌리면 → 죽은 채로 스폰
- `Hit` 트리거를 안 지우면 → **스폰하자마자 피격 모션 재생**

#### 2) 풀링이 만든 버그를 잡음

씬에 **직접 배치된 적 4마리**(`RL_Swampy`, `_1`, `_2`, `_3`)가 있었습니다.
`ObjectPool.Release` 는 자기가 만들지 않은 오브젝트를 경고만 내고 무시하기 때문에,
이들이 죽으면 **파괴되지 않고 화면에 남는 좀비**가 됩니다.

풀 출신인지 기억해서(`_fromPool`) 아니면 `Destroy` 로 폴백하게 고쳤습니다.

> **아침에 확인해주세요**: 그 4마리는 아직 씬에 남아 있습니다. 테스트하시다 놓아두신
> 것 같은데, 게임 시작하자마자 근처에 적 4마리가 서 있게 됩니다. 필요 없으면
> Hierarchy 에서 지우셔도 됩니다. 제가 임의로 지우지는 않았습니다.

#### 3) 한글이 □ 로 깨지던 버그 (사용자 눈에 보이는 문제)

새로 만든 카드 텍스트의 한글 일부가 네모로 나왔습니다. (`투` `척` `까` `적` `에` `던` …)

원인은 **글자를 담아둘 판(아틀라스)이 꽉 찬 것**이었습니다.
`Galmuri11 SDF` 는 Dynamic 모드라 글자를 런타임에 그때그때 만들어 이 판에 붙이는데,
1024×1024 판에 90pt 글자가 **104자까지만** 들어가고 그 뒤로는 만들지 못합니다.

**처음 시도한 방법이 틀렸습니다.** Multi Atlas Textures(판을 여러 장 쓰기)를 켰더니
글자 수는 늘었는데 **글자가 아예 안 보이게** 됐습니다 (사용자 제보: "카드는 보이는데
글자가 안보여"). 이유는 재질(Material)의 `_MainTex` 가 **판 1장만** 가리키고 있어서,
2번째 판에 붙은 글자는 화면에 그릴 텍스처가 없기 때문입니다.

**최종 해결**: Multi Atlas Textures 를 다시 끄고, **판 하나를 1024 → 2048×2048 로
키웠습니다.** 넓이가 4배가 되므로 넉넉합니다.
실측: 카드에 쓰이는 서로 다른 글자 125자 전부 정상 표시, 텍스트 메시 정점 수 > 0 확인.

> 앞으로 한글 텍스트를 더 추가하셔도 이제 안 깨집니다.
> 혹시 나중에 또 □ 가 보이면 **Multi Atlas 를 켜지 마시고** 아틀라스 크기를
> 2048 → 4096 으로 키우시면 됩니다.

#### 4) InputHandler 도메인 리로드 버그 (기존 버그)

에디터에서 스크립트를 재컴파일하면 `OnEnable` 은 다시 불리는데 `Awake` 는 다시
불리지 않아 `_inputActions` 가 null 인 채로 NRE 가 났습니다.

제가 건드린 코드는 아니지만, 컴파일할 때마다 이 에러가 떠서 **제 검증 루프가
다른 진짜 문제와 구분을 못 하는 상황**이라 고쳤습니다.
`EnsureInitialized()` 를 `Awake`/`OnEnable` 양쪽에서 부르게 한 3줄짜리 수정입니다.

---

### C11 — 네 번째 무기 '부메랑 도끼' (카드 22종)

`BoomerangProjectile` 이 `Projectile` 을 **상속**해서 피격 판정·관통·풀 반납·`IPoolable`
구현을 전부 물려받습니다. 재정의한 건 **"어떻게 움직이는가" 하나뿐**입니다.

- 나가는 중: 설정 거리까지 직진 (관통 2)
- 방향 전환: 그 지점에서 되돌아오며 타격 기록을 비움 → **왕복하며 2번 타격**
- 돌아오는 중: 주인을 쫓아가다 가까워지면 풀 반납
- 주인이 사라졌으면 즉시 회수 (떠도는 오브젝트 방지)

피해 18 / 주기 2.0초 / 왕복거리 5. 왕복 2히트라 주기가 길고 한 방이 셉니다.

실측: 왕복 10유닛을 속도 7로 = 약 1.4초, 실제 1.5초로 일치.
데미지 숫자에 `[18]` 이 두 번 떠서 2히트 확인.

---

### C12 — 적 행동 유형 3종 + 원거리형 적

적 4종이 전부 직진만 해서 **회피할 대상이 없던** 문제를 해결했습니다.

| 적 | 행동 | 동작 |
|---|---|---|
| 슬라임 · 오크전사 · 거대좀비 | Chase | 직진 추격 (기존) |
| 고블린 | **Charger** | 4.5거리에서 멈춰 0.45초 준비 → 3.5배속 돌진 → 2.2초 쿨다운 |
| 오크 주술사 (신규) | **Ranged** | 거리 6 유지, 2.2초마다 원거리 공격 |

**고블린의 준비 동작이 핵심입니다.** 돌진 직전에 멈춰 서기 때문에 피할 수 있고,
그게 없으면 그냥 빠른 적일 뿐입니다. 이 시간을 줄이면 훨씬 어려워집니다
(`RLGoblinSO.chargeWindup`).

**새 클래스를 거의 안 만들었습니다.** 행동은 `EnemySO` 데이터로 넣어 `RoguelikeEnemy` 가
분기하고, 적 투사체는 기존 `Projectile` 에 **대상 태그 필드 하나만 추가**해서 재사용했습니다.
(플레이어 무기는 `Enemy`, 적의 공격은 `PLAYER` 를 맞힘)

풀 재사용 대응도 챙겼습니다. `OnSpawnFromPool` 에서 돌진 상태를 초기화하지 않으면
**돌진 도중에 죽은 적이 다음 스폰 때 돌진 상태로 튀어나옵니다.**

---

### C13 — 밸런스: 1초 만에 죽던 문제

콘텐츠가 다 들어간 뒤 **최대 난이도에서 실제로 얼마나 버티는지** 계산해봤더니
플레이어가 **1초 만에 죽는** 상태였습니다.

**계산 (수정 전)**

```
큰 좀비 접촉 피해 40
  × 최대 난이도 배율 2.5   = 100
  − 플레이어 방어력 10     =  90
무적 시간이 0.6초 → 초당 90 ÷ 0.6 = 초당 150

플레이어 최대 체력 150  →  정확히 1.0초
```

게다가 **게임 전체에 회복 수단이 하나도 없었습니다.** 한 번 깎인 체력은
그 판이 끝날 때까지 영원히 돌아오지 않습니다. 무한 생존 게임에서 이건 치명적입니다.

**고친 것**

| 무엇 | 전 | 후 |
|---|---|---|
| `EnemySpawner._damageScaleEnd` (최대 난이도 피해 배율) | 2.5 | **1.6** |
| `RLBigZombieSO.attackDamage` | 40 | **26** |
| 체력 회복 수단 | 없음 | **'체력 재생' 카드 추가** |

**'체력 재생' 카드** — +0.8/초, 최대 5레벨 (다 모으면 4.0/초). 카드 **23종**이 됐습니다.

구현에서 한 가지 주의한 점: 매 프레임 소수점 단위로 회복시키면 **HP바가 계속 떨립니다.**
그래서 회복량을 버퍼에 쌓아두고 **1 이상 모였을 때만** 실제로 회복시킵니다
(`RoguelikePlayer.TickRegen()`).

**결과 (최악의 경우 — 큰 좀비에 계속 밀착)**

```
초당 피해   150  →  53
생존 시간   1.8초 →  5.2초   (체력 카드를 최대로 먹었을 때)
```

**검증**: 스포너를 끄고 적을 전부 비활성화한 상태에서 회복만 측정했습니다.

```
9.86초 동안  HP 50 → 73.0   (실측 회복량 23.0)
기대값       2.4/초 × 9.86초 = 23.7
HP바 fillAmount 0.487  =  HP/MaxHp 0.487   (정확히 동기화)
```

> **아침에 확인해주세요 — 이건 제가 임의로 정한 숫자입니다**
>
> `_damageScaleEnd` **1.6** 과 큰 좀비 공격력 **26** 은 위 계산에 맞춰 제가 고른 값입니다.
> "5초 정도는 버텨야 피할 기회가 있다"를 기준으로 잡았는데, 취향에 따라 다릅니다.
>
> - **더 어렵게**: 씬의 `EnemySpawner` → `Damage Scale End` 를 2.0~2.5 로 올리기
> - **더 쉽게**: `Assets/02_Scripts/Character/Data/체력 재생.asset` 의 회복량을 1.2 로 올리기
>
> 둘 다 인스펙터에서 숫자만 바꾸면 되고, 코드는 건드릴 필요 없습니다.

---

### C14 — 타격감 2차: 넉백 · 사망 연출 · 코인 흩뿌리기

적을 때렸을 때 화면상 변화가 **색이 잠깐 붉어지는 것뿐**이라, 맞고 있는 건지
그냥 지나가는 건지 구분이 잘 안 됐습니다. 세 가지를 추가했습니다.

#### 1) 피격 넉백 — 맞으면 뒤로 밀린다

맞는 순간 **플레이어 반대 방향으로 0.35만큼** 밀려나고, 밀리는 0.09초 동안은
스스로 움직이지 않습니다.

여기서 한 가지 함정이 있었습니다. `FixedUpdate` 에서 넉백 중일 때 **빠져나가지
않으면** 바로 아래 이동 코드가 매 프레임 속도를 덮어써서 **넉백이 화면상 전혀
보이지 않습니다.** 속도는 분명 설정되는데 다음 프레임에 지워지기 때문입니다.

> **왜 "때린 쪽"이 아니라 "플레이어 반대 방향"인가**
>
> 때린 쪽 위치를 알려면 `TakeDamage(피해량, 때린위치)` 처럼 인자를 늘려야 하는데,
> 이 함수는 **구 GamePlay 씬의 적도 함께 쓰는 약속(인터페이스)** 이라 손대면
> 그쪽이 깨집니다. 대신 플레이어 반대 방향으로 밀었습니다.
> 공전 칼날과 오라는 플레이어 몸에서 나오고, 단검과 도끼도 플레이어가 던지므로
> 사실상 모든 피해가 플레이어 쪽에서 옵니다. 결과가 거의 같습니다.

적마다 저항값을 줬습니다 (`EnemySO.knockbackResist`, 0이면 그대로 밀리고 1이면 꿈쩍 안 함).

| 적 | 저항 | 의도 |
|---|---|---|
| 거대 좀비 | 0.7 | 덩치값. 밀어내기 어려운 벽 역할 |
| 오크 전사 | 0.25 | 약간 버팀 |
| 오크 주술사 | 0.1 | 거의 그대로 밀림 |
| 슬라임 · 고블린 | 0 | 시원하게 날아감 |

#### 2) 사망 연출 — 부풀었다 쪼그라들며 사라진다

그냥 사라지면 **때려서 죽은 건지 화면 밖으로 나간 건지** 알기 어렵습니다.
0.14초 동안 **1.25배까지 부풀었다가 0으로 쪼그라들며 투명해진 뒤** 사라집니다.

이 0.14초 동안 적은 이미 "죽은" 상태라 **움직이지도, 몸으로 피해를 주지도 않습니다.**
연출 때문에 잠깐 남아 있어도 플레이어가 손해 보지 않습니다.

> **오브젝트 풀에서 제일 위험했던 부분**
>
> 적은 죽어도 파괴되지 않고 창고(풀)에 들어갔다가 재사용됩니다. 그런데 사망 연출이
> 끝난 시점의 적은 **크기 0 / 완전 투명 / 체력바 꺼짐** 상태입니다.
> 이걸 되돌리지 않고 반납하면 다음에 **보이지 않는 적**이 스폰됩니다.
> 스폰할 때와 반납할 때 양쪽에서 원래 상태로 되돌립니다.

#### 3) 코인 흩뿌리기

적이 뭉쳐서 죽으면 코인이 **완전히 같은 자리에 겹쳐** 한 개처럼 보였습니다.
반경 0.25 안에서 조금씩 흩뿌립니다.

#### 실측 검증

```
넉백    맞은 직후 속도 2.50 -> 3.89   (= 밀리는거리 0.35 ÷ 밀리는시간 0.09, 설계값 일치)
        방향은 플레이어 반대쪽과 일치도 1.000 (완전히 반대방향)

사망    5마리 동시 처치 -> 화면의 적 36 -> 31
        창고에 있는 257개 전수 검사: 크기·투명도·체력바가 어긋난 적 0개

코인    완전히 같은 좌표에서 6마리를 죽였을 때
        코인 6개가 간격 0.040 ~ 0.394 로 흩어짐. 완전히 겹친 쌍 0
```

> **취향에 따라 조절하실 곳**: 적 프리팹의 `RoguelikeEnemy` 컴포넌트에
> `Knockback Distance`(밀리는 거리), `Death Pop Duration`(사망 연출 길이),
> `Coin Scatter Radius`(코인 흩뿌리기)가 인스펙터에 나와 있습니다.
> 사망 연출을 0으로 두면 예전처럼 즉시 사라집니다.
